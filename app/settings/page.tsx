"use client";
import{AppShell}from"@/components/app-shell";import{useAuth}from"@/components/auth/auth-provider";import{ThemeToggle}from"@/components/theme-toggle";import{signOut}from"firebase/auth";import{auth}from"@/lib/firebase";
export default function Page(){const{user}=useAuth();return <AppShell><section className="page narrow"><div className="page-head"><div><p className="eyebrow">Account</p><h1>Settings</h1><p>Manage your account and appearance.</p></div></div><div className="settings-card"><div className="avatar large">{(user?.displayName?.[0]||"S").toUpperCase()}</div><div><h2>{user?.displayName||"Student"}</h2><p>{user?.email}</p></div></div><div className="settings-actions"><ThemeToggle/><button className="secondary-button" onClick={()=>signOut(auth)}>Sign out</button></div></section></AppShell>}
