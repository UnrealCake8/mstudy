"use client";
import Link from"next/link";import{GraduationCap}from"lucide-react";import{AppShell}from"@/components/app-shell";import{useAuth}from"@/components/auth/auth-provider";import{ThemeToggle}from"@/components/theme-toggle";import{signOut}from"firebase/auth";import{auth}from"@/lib/firebase";
export default function Page(){const{user}=useAuth();return <AppShell><section className="page narrow"><div className="page-head"><div><p className="eyebrow">Account</p><h1>Settings</h1><p>Manage your account, appearance and connected school tools.</p></div></div><div className="settings-card"><div className="avatar large">{(user?.displayName?.[0]||"S").toUpperCase()}</div><div><h2>{user?.displayName||"Student"}</h2><p>{user?.email}</p></div></div><Link href="/classroom" className="settings-card settings-link"><div className="connect-icon small"><GraduationCap size={20}/></div><div><h2>Google Classroom</h2><p>Connect, sync or disconnect your classes and assignments.</p></div><span className="settings-arrow">→</span></Link><div className="settings-actions"><ThemeToggle/><button className="secondary-button" onClick={()=>signOut(auth)}>Sign out</button></div></section></AppShell>}
