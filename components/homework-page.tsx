"use client";

import { FormEvent, useEffect, useMemo, useState } from "react";
import { Check, Plus, Trash2 } from "lucide-react";
import { useAuth } from "@/components/auth/auth-provider";
import { addItem, deleteItem, Homework, subscribeCollection, updateItem } from "@/lib/data";

export function HomeworkPage(){const{user}=useAuth();const[items,setItems]=useState<Homework[]>([]);const[open,setOpen]=useState(false);useEffect(()=>user?subscribeCollection<Homework>(user.uid,"homework",setItems):undefined,[user]);const pending=useMemo(()=>items.filter(i=>!i.completed).sort((a,b)=>a.dueDate.localeCompare(b.dueDate)),[items]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!user)return;const f=new FormData(e.currentTarget);await addItem(user.uid,"homework",{title:String(f.get("title")),subject:String(f.get("subject")||"General"),dueDate:String(f.get("dueDate")),priority:String(f.get("priority")||"medium"),completed:false});e.currentTarget.reset();setOpen(false)}return <section className="page"><div className="page-head"><div><p className="eyebrow">Planner</p><h1>Homework</h1><p>{pending.length} task{pending.length===1?"":"s"} still to do.</p></div><button className="primary-button" onClick={()=>setOpen(v=>!v)}><Plus size={17}/> Add homework</button></div>{open&&<form className="editor-card compact" onSubmit={submit}><input name="title" placeholder="What do you need to do?" required/><input name="subject" placeholder="Subject"/><input name="dueDate" type="date" required/><select name="priority" defaultValue="medium"><option value="low">Low priority</option><option value="medium">Medium priority</option><option value="high">High priority</option></select><button className="primary-button">Save task</button></form>}<div className="task-list">{items.length===0?<div className="empty-state"><strong>You’re all caught up</strong><span>Add homework when you get it.</span></div>:items.sort((a,b)=>a.dueDate.localeCompare(b.dueDate)).map(i=><article className={i.completed?"task-row completed":"task-row"} key={i.id}><button className="check-button" onClick={()=>user&&updateItem(user.uid,"homework",i.id,{completed:!i.completed})}>{i.completed&&<Check size={16}/>}</button><div className="task-copy"><strong>{i.title}</strong><span>{i.subject} · Due {i.dueDate}</span></div><span className={`priority ${i.priority}`}>{i.priority}</span><button className="icon-button danger" onClick={()=>user&&deleteItem(user.uid,"homework",i.id)}><Trash2 size={17}/></button></article>)}</div></section>}
